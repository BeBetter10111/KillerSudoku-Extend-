/**
 * WebSocket adapter stub — infrastructure outbound.
 */
export function createGameSocket() {
  return {
    connect() {
      // Placeholder
    },
    disconnect() {
      // Placeholder
    },
    /**
     * @param {(event: { eventType: string }) => void} _handler
     */
    onEvent(_handler) {
      // Placeholder
    },
  };
}
